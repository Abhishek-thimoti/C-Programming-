#include<stdio.h>
int main()
{
	//Program to check whether a triangle is equilatoral, Isosceles or Scalence.
	int a,b,c;
	printf("Enter the side of Triangle :\n");
	scanf("%d%d%d",&a,&b,&c);
	
	if(a=b=c)
	{
		printf("Triangle is equilatoral ");
	}
	else if (a=b!=c || a!=b=c || a )
	return 0;
}
